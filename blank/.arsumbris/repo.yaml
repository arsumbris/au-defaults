# The minimal curated empty start: the host UI + agent layer mounted, nothing composed.
type: au.engine.repo::au-engine
name: blank
description: the curated empty start — host UI and agent layer, nothing composed
deps:
  - name: au-host-sdk
